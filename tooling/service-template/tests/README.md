# Tests

Each executable service must define tests appropriate to its boundary. Prefer fast domain/application unit tests and targeted integration tests for persistence, transport, and external adapters.

# Configuration

Runtime configuration belongs here and must be validated before the service starts. Secrets must come from the runtime environment/secret manager and must never be committed to source control.

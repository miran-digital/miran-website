name: Web Container

on:
  pull_request:

permissions:
  contents: read

jobs:
  build-web:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Build storefront production image
        run: docker build -f apps/web/Dockerfile -t miran-web:ci .

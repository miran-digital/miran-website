# Shared packages

This directory is for narrowly scoped technical packages shared by applications and services.

Allowed examples include API contracts, observability primitives, configuration helpers, error conventions, and shared lint/TypeScript configuration.

Business-domain persistence models must not be centralized here because that would break service data ownership.

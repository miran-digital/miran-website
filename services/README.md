# Services

Deployable backend services live in this directory.

Phase 1 is limited to `api-gateway`, `auth-service`, `catalog-service`, and `admin-service`.

Future services must follow the architectural rules in `docs/architecture/` and the standard in `tooling/service-template/`. A future service must not be added simply because it exists on the roadmap; it must have a justified business or scaling boundary.

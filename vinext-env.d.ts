// Keep vinext's Next.js-compatible public API declarations available in a
// clean checkout. `next-env.d.ts` is generated locally and intentionally
// ignored, so production type checks must not depend on that generated file.
import "vinext/types";
